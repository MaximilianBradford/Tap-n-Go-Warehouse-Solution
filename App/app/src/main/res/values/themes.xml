<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- App Theme -->
    <style name="Theme.TapNGo" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary Colors -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primary_variant</item>
        <item name="colorOnPrimary">@color/on_primary</item>

        <!-- Secondary Colors -->
        <item name="colorSecondary">@color/secondary</item>
        <item name="colorSecondaryVariant">@color/secondary_variant</item>
        <item name="colorOnSecondary">@color/on_secondary</item>

        <!-- Background Colors -->
        <item name="android:windowBackground">@color/background</item>
        <item name="android:statusBarColor">@color/primary_variant</item>

        <!-- Text Styles -->
        <item name="android:textColorPrimary">@color/on_background</item>
        <item name="android:textColorSecondary">@color/on_surface</item>

        <!-- Customize Toolbar/ActionBar -->
        <item name="android:actionBarStyle">@style/Widget.AppCompat.Toolbar</item>
    </style>

    <!-- Splash Screen Theme -->
    <style name="Theme.Splash" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Splash Screen Background -->
        <item name="android:windowBackground">@drawable/ic_splash_background</item>
        <!-- Fullscreen on Splash Screen -->
        <item name="android:windowFullscreen">true</item>
        <!-- Customize ProgressBar and Splash UI -->
        <item name="android:progressBarStyle">@android:style/Widget.ProgressBar</item>
        <!-- Customize status bar color (optional) -->
        <item name="android:statusBarColor">@android:color/black</item>
    </style>

</resources>
